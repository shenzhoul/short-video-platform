/**
 * Shared pagination constants used across components
 */

/** Default page size for paginated lists */
export const DEFAULT_PAGE_SIZE = 10;

/** Common ID field names used for cursor pagination */
export const CURSOR_ID_KEYS = ['_id', 'id', 'uid'];

/** Common date field names used for cursor pagination */
export const CURSOR_DATE_KEYS = ['createdAt', 'updatedAt', 'created_at', 'updated_at'];

export const POST_PAGE_LIMIT = 20;
